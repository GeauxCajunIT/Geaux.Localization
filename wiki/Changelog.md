# Changelog

## Unreleased
- Introduced `AddGeauxLocalization(IConfiguration, Action<GeauxLocalizationOptions>)`.
- Unified configuration into `GeauxLocalizationOptions`.
- Added startup hosted service for `AutoMigrate` and `AutoSeed` behavior.
- Standardized `IDbContextFactory` wiring.

## 1.0.0
- Initial release.
